<div class="past-reviews-container">
  <h2>Past reviews</h2>
  <ul class="list-group">
    <% @service.reviews.each do |review| %>
      <li class="list-group-item">
        <div class="reviews"><%= "'#{review.comment}'- " %>
          <%= review.user.username %>
        </div>
      </li>
    <% end %>
  </ul>
</div>
